import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  input: {
    borderColor: "#444444ff",
    borderWidth: 1,
    padding: 10,
    marginVertical: 10,
    borderRadius: 5,
  },
});
